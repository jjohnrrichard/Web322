<!DOCTYPE html>
<html lang="en" data-theme="forest">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= project.title %> | Climate Project</title>
  <link href="/css/main.css" rel="stylesheet" />
</head>
<body>
<%- include('partials/navbar', { page: '' }) %>

<div class="container mx-auto my-10">
<div class="hero bg-base-200 rounded-lg p-10 mb-6">
     <div class="hero-content text-center">
     <div class="max-w-xl">
          <h1 class="text-4xl font-bold"><%= project.title %></h1>
          <p class="py-4">Below, you will find detailed information about the project.</p>
        </div>
      </div>
    </div>

<div class="max-w-4xl mx-auto">
    <img src="<%= project.feature_img_url %>" alt="Image" class="rounded-lg shadow mb-6 w-full" />
      <p class="mb-4"><strong>Introduction:</strong> <%= project.intro_short %></p>
      <p class="mb-4"><strong>Impact:</strong> <%= project.impact %></p>
      <p class="mb-6">
        <a class="btn btn-secondary" href="<%= project.original_source_url %>" target="_blank">More Info</a>
      </p>

      <blockquote class="border-l-4 border-primary pl-4 italic text-gray-600">
        <p id="quote">Loading quote...</p>
        <p id="author" class="text-right font-semibold text-sm mt-2"></p>
      </blockquote>

      <div class="mt-10">
        <a href="#" onclick="history.back(); return false;" class="btn btn-outline">← Back</a>
      </div>
    </div>
  </div>

  <script>
    document.addEventListener("DOMContentLoaded", () => {
      fetch("https://dummyjson.com/quotes/random")
        .then(res => res.json())
        .then(data => {
          document.getElementById("quote").textContent = `"${data.quote}"`;
          document.getElementById("author").textContent = `– ${data.author}`;
        })
        .catch(err => {
          document.getElementById("quote").textContent = "Failed to load quote.";
        });
    });
  </script>

</body>
</html>